#!/usr/bin/env bash

function exitstatus_prompt {
  status=$?
  if [ "$status" -eq 0 ]
  then
    # Yay, everything went great
    echo -e "${1}"
  else
    # The last command broke my heart...
    echo -e "(${txtred}${status}${txtrst})""${2}"
  fi
  return $status
}